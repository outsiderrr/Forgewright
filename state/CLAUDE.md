# state 模块补充规则

## 本模块职责（一句话）

维护世界本体（单一事实源）与运行时状态总线的读写 API。

## 硬性约束（继承自 /CLAUDE.md）

- LLM 永远不能直接写状态；变更只能由确定性代码经白名单 intent 执行（见 ADR-006、ADR-008）。
- 运行时路径只用读 API；写 API 仅供生产期调用（见 ADR-004）。

## TODO

阶段 0 启动时补充：本体目录结构、状态总线事件模型、读写接口边界。
