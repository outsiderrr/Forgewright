# L1 治理会话起手 prompt —— ABC 阶段层级化提案

> **类型**：L1 / 治理类起手 prompt（非 L3 任务 prompt，不走 §11 的 T-N.X 工作流）。
> **状态**：提案（PROPOSED），未拍板、未应用。本文件是「将来起 L1 治理会话时的起手 prompt」，存档备用。
> **配套提案全文**：`~/Desktop/brain/提案-Forgewright-ABC阶段层级化.md`（作者个人 vault，当前不在本仓库；如需仓库自包含，可把提案复制进 `docs/` 并改下方引用路径）。
> **用法**：准备正式推进时，在本仓库起一个**全新**的 Claude Code 会话，把下面分隔线内的内容作为第一条消息。

---8<--- 起手 prompt 从这里开始 ---8<---

这是一个 L1 治理会话（架构/治理层），独立于任何 dev 会话。你的任务是把一份治理变更【提案】评审、压力测试、并（在我明示授权后）落成 ADR + governance 下一版。

第一步，先按顺序读这些：
1. ~/Desktop/brain/提案-Forgewright-ABC阶段层级化.md —— 待评审的提案全文（在我个人 vault，不在仓库内）。
2. CLAUDE.md（项目根）—— 硬规则，尤其规则 2 / 9 / 10。
3. docs/governance.md —— 现行治理 v0.4.1，重点 §1 / §2 任务分类 / §3 review tier / §10 ABC 流程 / §11。
4. docs/DECISIONS.md 里的 ADR-015（schema 串行 commit），以及任何跟 ABC / schema 相关的 ADR。
5. 扫一眼 docs/ROADMAP.md 的当前阶段（阶段 3）。

提案一句话：把 ABC 三阶段从「固定在每个 L3」改为「放在最具体且有规划的层级」；并用「设计先于施工」把 schema（地基）改动挡在施工之前。详见提案 §0–§2。

你的工作，按这个顺序：

1) 复述确认：用你自己的话把提案讲一遍，确认理解对了，并指出它触动了现行治理的哪几条（§10 / ADR-015 / §2 分类 / §11 跳 BC 破例 等）。

2) 对抗性评审（重点，别当橡皮图章）：这份提案来自一个 dev 对话会话，可能带乐观偏见。请主动找它会崩的地方——
   - 提案 §7 列了 5 个开放问题，逐个给出你的判断和建议；
   - 额外找：它跟哪条现有 ADR / 治理条款冲突或需要改写？「设计先于施工」在实操里最可能在哪失效？「攒批」还有哪些隐藏成本？
   - 对每个风险给「接受 / 缓解 / 否决」的明确建议 + 理由。

3) 产出草稿（先不 commit、先不改任何文件，只在对话里给我看）：
   - 一份新 ADR 草稿（编号留 ADR-0XX 待我定）：决策 + 理由 + 被否决的替代方案；
   - 一份 governance vNext 的 diff 草案（具体改 §10 / §3 / §2 哪几处）；
   - 一份给 cross-LLM（GPT-5.5 / Codex）的 critique prompt，我会拿去另一个模型跑独立评审（按 governance §5 的 cross-LLM 流程）。

4) 然后停下来等我：在我明示授权之前，绝不修改 CLAUDE.md / docs/governance.md / docs/DECISIONS.md 任何 L1 文档（CLAUDE.md 规则 9/10）。我跑完 cross-LLM critique、消化后，再告诉你怎么落地。

两条硬约束：
- 这是治理设计，不是写代码。别碰 /schema、/engine、/state 等模块代码。
- 不确定的架构问题，停下来问我，别自己拍板（CLAUDE.md 规则 7）。

现在只做第 1 步（读 + 复述确认），然后停下来让我确认，再往下走。

---8<--- 起手 prompt 到这里结束 ---8<---
