# Code Review — T-1.5.1 — ADR-014 + ROADMAP 1.5 实质 + SCHEMA_v0.2.md 占位 + visuals/_reference/ 目录

**评审者**：GPT-5.5 via Codex
**评审日期**：2026-04-30
**评审范围**：77a5f54（7 文件 / +136/-4 行）
**项目阶段**：阶段 1.5（视觉资产生成）

---

## 1. 一句话结论
主文档方向基本对齐阶段 1.5，但 commit 里带入了一个不在本轮授权边界内的旧格式评审准备文件；按模块边界纪律，不建议无记录放行。边界内另有 2 处文档语义不一致，修起来很小，但会影响后续 Claude 对 Schema 路径和 manual 背景生成契约的理解。

## 2. 严重度分布
| 严重度 | 数量 | 边界内可修 | 跨边界 |
|---|---:|---:|---:|
| 🔴 CRITICAL | 1 | 0 | 1 |
| 🟡 IMPORTANT | 2 | 2 | 0 |
| 🟢 NICE | 0 | 0 | 0 |
| **合计** | 3 | 2 | 1 |

## 3. 必修（🔴 CRITICAL）

### 3.1 [ARCH] docs/reviews/_targets/T-1.5.1_review_target.md:17 — 提交了不在本任务授权清单内的旧格式评审准备文件
**问题**：本文件把 `/docs/reviews/_targets/T-1.5.1_review_target.md` 自列为本任务允许新增的文件，但用户给出的 T-1.5.1 修复边界只允许 6 个路径：`DECISIONS.md`、`ROADMAP.md`、`SCHEMA_v0.2.md`、`_reference/.gitkeep`、`content/visuals/CLAUDE.md`、`.gitignore`。同时，`docs/STAGE_1.5_TASKS.md:504` 要求的是 `/docs/reviews/_prompts/T-1.5.1_codex_review.md`，不是 `_targets/` 旧格式文件。这样会让后续执行会话以为可以靠准备文件自行扩展边界。
**为什么 CRITICAL**：违反 `CLAUDE.md` 规则 2 的模块边界纪律；本任务又是纯文档架构准备，边界本身就是验收对象之一。
**建议修复**：
```markdown
# 当前
- `/docs/reviews/_targets/T-1.5.1_review_target.md`（**新建**本文件；评审准备）

# 改为
- 不在 T-1.5.1 主 commit 内提交 `_targets/` 旧格式准备文件。
- 如仍需要评审准备文件，按当前阶段工作流在单独授权下产出 `/docs/reviews/_prompts/T-1.5.1_codex_review.md`。
```
**模块边界状态**：⚠️ 跨边界（不要在本修复轮直接删改 `docs/reviews/_targets/`；先记 CLEANUP 或请作者单独授权）

## 4. 应修（🟡 IMPORTANT）

### 4.1 [ARCH] docs/ROADMAP.md:106 — Schema 路径同时写成 C 和 A
**问题**：同一个阶段 1.5 段里，完成标志写“路径 C”，但重点工作和禁止事项又写 “path A”（见 `docs/ROADMAP.md:114` 与 `docs/ROADMAP.md:125`）。`docs/STAGE_1.5_TASKS.md` 锁定表采用的是 Path A：仅扩展数据、不正式化角色 Schema。两套标签同时存在，会让 T-1.5.2 的执行会话误判 Schema 授权范围。
**为什么 IMPORTANT**：Schema 扩展是阶段 1.5 串行关键路径；路径名不一致不是措辞小错，而是会影响“是否正式化角色 Schema”的边界判断。
**建议修复**：
```markdown
# 当前
- Schema 已扩展：本体角色实体新增 `visual_assets` 字段（已授权动 Schema，路径 C）

# 改为
- Schema 已扩展：本体角色实体新增 `visual_assets` 字段（已授权动 Schema；阶段 1.5 执行 path A：仅扩展数据，不正式化角色 Schema）
```
**模块边界状态**：✅ 在边界内（可修）

### 4.2 [ARCH] docs/DECISIONS.md:276 — ADR-014 的 manual 两段式契约只点名 character_sheet
**问题**：ADR-014 的“决策”段只写 `generate_character_sheet(mode='manual')` 产出 prompt 包，但 ROADMAP 完成标志同时要求 `generate_scene_background(location_ref)` 和 manual 路径全跑通；ADR 自己的后果段 `docs/DECISIONS.md:289` 也说两个函数在 manual 模式下都变两段式。决策段漏掉背景生成入口，会让后续实现者只把 manual 契约落到角色立绘。
**为什么 IMPORTANT**：ADR 的“决策”段是后续实现的硬契约；若背景生成不走同一 two-stage manual 流程，阶段 1.5 的 manual 验收路径会不完整。
**建议修复**：
```markdown
# 当前
- **manual 模式契约（两段式）**：第一段 `generate_character_sheet(mode='manual')` 产出 prompt 包到 `/content/visuals/_pending/<asset_id>/`；作者人工生成下载；第二段 `image_import` CLI 扫描 → 校验 → 入库

# 改为
- **manual 模式契约（两段式）**：第一段 `generate_character_sheet(mode='manual')` / `generate_scene_background(mode='manual')` 产出 prompt 包到 `/content/visuals/_pending/<asset_id>/`；作者人工生成下载；第二段 `image_import` CLI 扫描 → 校验 → 入库
```
**模块边界状态**：✅ 在边界内（可修；属于补齐 ADR-014 内部一致性，不改变决策实质）

## 5. 可选改进（🟢 NICE）
无

## 6. 已知遗留项核对
无

## 7. Top 3 行动优先级（按 ROI）
1. **3.1**：先处理越界 `_targets/` 文件的归属，否则后续会话会继续继承错误边界。
2. **4.1**：统一 ROADMAP 中 Schema 路径标签，避免 T-1.5.2 在最敏感的 Schema 任务上误读授权。
3. **4.2**：补齐 ADR-014 manual 契约里的 `generate_scene_background`，让背景生成和角色立绘共享同一两段式流程。

## 8. 评审范围外的观察（可忽略）
- 当前工作树有未跟踪的 `docs/STAGE_1.5_TASKS.md` 和 `docs/reviews/_prompts/T-1.5.1_codex_review.md`；前者按本评审 prompt 属正常，后者不在 commit 77a5f54 范围内，本报告不把它当 finding。
- 未运行 pytest：本次 commit 是纯文档 + `.gitignore`。已运行 `git diff --check 77a5f54^ 77a5f54`，无空白错误输出。

## 9. 修复任务指令（**给原 Claude Code 会话**）

> 作者：把下面 ` ```text` 代码块**整段**复制到新 Claude Code 会话作为首条消息。Claude 会读评审报告 + 修代码 + commit + push。

```text
你的任务是修复 T-1.5.1 评审中发现的问题。评审报告在 /docs/reviews/2026-04-30_T-1.5.1_review.md，先读它再开始。

# 必读
1. /docs/reviews/2026-04-30_T-1.5.1_review.md（评审报告全文；finding 清单 / 建议修复 diff / 边界状态）
2. /CLAUDE.md
3. /docs/STAGE_1.5_TASKS.md T-1.5.1 章节
4. /docs/DECISIONS.md（特别 ADR-011 / ADR-012 / ADR-014）

# 模块边界（与原 T-1.5.1 任务相同；评审报告 §3/§4/§5 内 ⚠️ 跨边界 项**不要修**）
只允许修改 / 新建：
  - /docs/DECISIONS.md
  - /docs/ROADMAP.md
  - /docs/SCHEMA_v0.2.md
  - /content/visuals/_reference/.gitkeep
  - /content/visuals/CLAUDE.md
  - /.gitignore
严禁修改：CLAUDE.md、SCHEMA_v0.md、DEBATE_NOTES.md、HANDOFF_STAGE_1_TO_1.5.md、STAGE_1_ACCEPTANCE.md、任何代码模块、任何 /schema/*.json、/content/test_scene_v0/

# 待修 finding 清单

🔴 CRITICAL（必修，全部边界内的）：
无。§3.1 是跨边界项，不在本修复轮直接改。

🟡 IMPORTANT（建议全修；如某条修起来风险高 / 不确定，单独提出由作者决定）：
1. 4.1 [ARCH] docs/ROADMAP.md:106 — 统一 Schema 路径术语：把“路径 C”改为与 STAGE_1.5_TASKS.md 锁定决策一致的 path A 表述（仅扩展数据，不正式化角色 Schema）。边界内。
2. 4.2 [ARCH] docs/DECISIONS.md:276 — 在 ADR-014 的 manual 两段式契约中补上 `generate_scene_background(mode='manual')`，只补齐内部一致性，不改变 ADR 决策实质。边界内。

🟢 NICE（**默认跳过**；如有极简单的 1 行级修复可顺手做；否则不动）：
默认跳过（本报告无 NICE finding）。

⚠️ 跨边界 / 与 ADR 冲突的 finding（**不要修**；记入 /docs/CLEANUP.md 如不存在则创建）：
1. 3.1 [ARCH] docs/reviews/_targets/T-1.5.1_review_target.md:17 — `_targets/` 旧格式评审准备文件越界进入 commit，且当前工作流应使用 `_prompts/`。不要在本修复轮删除或改动 `docs/reviews/_targets/`；在 /docs/CLEANUP.md 记录“需作者单独授权清理 T-1.5.1 旧格式 review target / 确认是否改为 _prompts 工作流”。

# 修复纪律
- **最小变更**——只修该 finding；不要顺手重构 / 加抽象 / 优化别的
- **保留或新增测试**覆盖修复——本任务是纯文档，TEST 维度通常不涉及；如有需要补的格式校验/链接校验，作为最小补充
- **跑测试通过**——pytest 必绿（如有相关测试）；测试不通过**不要 commit**，向作者报告
- **每条 finding 单独 commit**，commit message：
  `fix(<module>): <一行修复内容> (review of T-1.5.1 #<finding 编号>)`
  末尾附 Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
- **全部修完后 push**

# 不要做的事
- 不要 disable / skip 测试来通过
- 不要修改 ADR / CLAUDE.md / 任何 schema_version / SCHEMA_v0*.md（注：SCHEMA_v0.2.md 在本任务边界内可修，但**已签字** ADR-014 不可改；只允许补充而不可改决策实质）
- 不要 amend 既有 commit；每个修复一个新 commit
- 不要 push --force；不要修改 git config
- 不要修改跨边界文件（即使评审报告写了建议修复 diff）
- 不要扩展修复范围到报告未列的问题（如发现新 bug，记入 /docs/CLEANUP.md，**不要**直接修；告知作者）
- 不要试图自己重新评审——你的工作是按报告执行修复

# 完成报告
- 已修 finding 列表 + commit hash 各一
- 未修 finding 列表 + 原因（NICE 跳过 / 跨边界已记 CLEANUP / 测试失败 / 其他）
- push 完成确认（git log --oneline -5 输出）
```
