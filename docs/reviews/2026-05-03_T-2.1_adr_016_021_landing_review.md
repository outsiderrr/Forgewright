# Code Review — PR Forgewright#7（commit df05431，base=main）—— X2 = T-2.1 ADR-016~021 立项

**评审者**：GPT-5.5 via Codex
**评审日期**：2026-05-03
**评审范围**：PR Forgewright#7（commit df05431，base=main）—— X2 = T-2.1 ADR-016~021 立项（统计：1 文件 / 188 行变更）
**项目状态**：阶段 2

---

## 1. 一句话结论

新增 ADR-016~021 的核心决策字面基本对齐 STAGE_2_TASKS §3，没有发现 schema_version、2A/2B 范围或 PR 改动范围的阻塞级偏离。建议修一处后果段依赖漏列后再合入，成本很低。

## 2. 严重度分布

| 严重度 | 数量 |
|---|---|
| 🔴 CRITICAL | 0 |
| 🟡 IMPORTANT | 1 |
| 🟢 NICE | 0 |
| **合计** | **1** |

## 3. 必修（🔴 CRITICAL）

无

## 4. 应修（🟡 IMPORTANT）

### 4.1 [ARCH] docs/DECISIONS.md:351 — 部分 ADR 后果段漏列已锁定下游任务依赖

**问题**：STAGE_2_TASKS §3 要求每条 ADR 后果行明示下游 L3 依赖，但当前新增 ADR 有几处只写了语义，没有完整列出 T 编号。ADR-016 后果列了 T-2.2 / T-2.7 / T-2.5，却漏 T-2.4；ADR-017 后果列了 T-2.5 / T-2.7，却漏 T-2.2；ADR-019 后果写了 schema 字段追加，但未明示 T-2.2。决策内容本身是对的，问题在依赖标注不完整。

**为什么是 IMPORTANT**：T-2.1 是后续 L3 任务的架构锚点；漏列依赖会让不会编程的作者和后续 Claude 会话在 pre-flight 时少 grep 一个 ADR，尤其 T-2.4 机械预检依赖 ADR-016 的 state path / state_path_slug，T-2.2 schema 落地依赖 ADR-017/019。

**修复建议**：

```markdown
# ADR-016 后果段补一行
- 机械预检器（T-2.4）必须按五个 state path namespace + state_path_slug 反查执行 path 前缀 / BOND_ID_UNKNOWN 检查

# ADR-017 后果段补一行
- schema 落地（T-2.2）必须新建 clock schema，使用 ticks_filled / ticks_total maximum 20 / event_based advance_rule 契约

# ADR-019 后果段补一行或改写现有第一行
- schema 落地（T-2.2）必须以 optional + additionalProperties 兼容路径追加 generation_trace.slot_assignments，dialogue_graph / node schema_version 保持 0.1.1
```

## 5. 可选改进（🟢 NICE）

无

## 6. 已知遗留项核对

阅读 /docs/STAGE_1_ACCEPTANCE.md §4 R1–R8 后，本次评审中遇到的下列 finding **属已知 R 项**，不重复列入正文：

无

## 7. Top 3 行动优先级

按"修起来 ROI 最高"排序：

1. 4.1：补齐 ADR-016 / 017 / 019 后果段的 T 编号依赖；这是纯文档小修，能避免后续 L3 会话漏接任务边界。

## 8. 评审范围外的观察（可忽略）

无
