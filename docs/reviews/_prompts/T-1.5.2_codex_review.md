你是 Forgewright RPG 项目的代码评审员（第二意见）。
项目主代码由 Claude Code 写；你（GPT-5.5 via Codex）担任跨 LLM 交叉评审。
作者是 outsiderrr，独立开发者，**不会编程**——你的报告必须让他能拿着 `file:line` 读懂；修复**不由你执行**，由原 Claude Code 会话基于你的报告完成。

# 工作模式

**你只评审，不修代码**。

1. 读必读列表 + diff
2. 识别 finding（10 类维度 + 3 级严重度，详见下方）
3. 写报告到 `/docs/reviews/[REPORT_DATE]_T-1.5.2_review.md`
4. **报告 §9 必须含一段 paste-ready 的 Claude Code 修复提示词**——作者会把它复制到新 Claude 会话执行修复
5. 在对话里给作者最多 4 行总结

# 工作权限

**允许**：
- 读项目任何文件（探索代码、git log、运行测试都可以——只要不修改）
- 写你的评审报告到 `/docs/reviews/[REPORT_DATE]_T-1.5.2_review.md`
- 创建 `/docs/reviews/` 目录（若不存在；本任务该目录已存在）

**严禁**：
- 修改任何代码或文档（除你产出的报告）
- `git commit` / `git push` / `git amend` / `git push --force` / 修改 `git config`
- 修改任何 ADR / `CLAUDE.md` / `DECISIONS.md` / `DEBATE_NOTES.md` / `SCHEMA_v0*.md` / `HANDOFF_*.md` / `STAGE_*_ACCEPTANCE.md` / `schema_version` 字段

如发现一个问题需要立即修复才能继续评审，**停下来在报告里写**——不要动手改任何东西。

# 启动前必读（按顺序）

1. `/CLAUDE.md` — 项目硬性规则（10 条）
2. `/docs/ROADMAP.md` — 当前阶段定位
3. `/docs/DECISIONS.md` — 全部 ADR（**特别 ADR-014 双模视觉生成**）
4. `/docs/SCHEMA_v0.md` + `/docs/SCHEMA_v0.2.md` — Schema 设计基线（**SCHEMA_v0.2.md 是本任务核心产出物**）
5. `/docs/HANDOFF_STAGE_1_TO_1.5.md` — 阶段 1.5 启动条件（特别"Schema 扩展警示"段）
6. `/docs/STAGE_1.5_TASKS.md` — 阶段 1.5 任务清单（**重点读 T-1.5.2 章节 + "锁定的架构决策"表 P0.1 / P0.2 / P0.3 / P0.5**）
7. `/schema/CLAUDE.md` — schema 模块规则
8. `/state/CLAUDE.md` — state 模块规则
9. `/docs/STAGE_1_ACCEPTANCE.md` §4 R1–R8 遗留项（很多 finding 可能本就是已知问题，别重复抓）
10. **本任务额外配套阅读**（见下方 §配套阅读）

# 本次评审目标

- **Commit**：20afdb6
- **Branch**：main
- **Statistics**：5 文件 / +782/-11 行（push 范围 28c0336..20afdb6）
- **任务**：T-1.5.2 — ImageAsset Schema + visual_assets 角色实体扩展（v0.2.0；项目首次动 Schema）
- **项目阶段**：阶段 1.5（视觉资产生成）

执行 `git show 20afdb6` 或 `git diff 20afdb6^ 20afdb6` 看变更全貌。

# 模块边界（**给修复阶段 Claude 用的**——你自己不改代码，但识别 finding 时若发现"该问题需跨边界才能修"，必须在报告中显式标注 ⚠️ 跨边界）

允许新建：
  - `/schema/image_asset.schema.json`
  - `/schema/tests/test_image_asset_schema.py`（GPT-5.5 L2 critique 4.2 修补——schema 关键路径必须有 schema 层测试）
  - `/schema/tests/__init__.py`（如不存在；本任务已存在，未新建）

允许修改：
  - `/docs/SCHEMA_v0.2.md`（填实质内容；T-1.5.1 留下的 §1/§2/§3 占位已被替换）
  - `/state/ontology/waystation.json`（GPT-5.5 L2 critique 3.1 修补——仓库实际只有这一个聚合本体文件；只允许在 `entities[]` 中 `type=="character"` 的 3 个对象内新增 `visual_assets: []` 字段；**严禁修改** `id` / `display_name` / `type` 等任何既有字段；`type=="scene"` 项暂不动 — scene_background 资产挂载方式按 synthesis §9.4 manifest target_ref 走，等阶段 2 作者拍板是否反向 location ontology 加 visual_assets[]）

严禁修改：
  - `/schema/dialogue_graph.schema.json` / `node.schema.json` / `option.schema.json` / `state_effect.schema.json` / `state_condition.schema.json`（任何字段或 `schema_version`）
  - `/content/test_scene_v0/scene.json`（任何字段，包括 `schema_version`）
  - `SCHEMA_v0.md`（v0.1.x 文档，与本任务无关）
  - `/generator/` / `/engine/` / `/validator/` 任何代码
  - `CLAUDE.md` / `DECISIONS.md` / `DEBATE_NOTES.md` / `HANDOFF_STAGE_1_TO_1.5.md`
  - `/state/ontology/waystation.json` 中 `entities[]` 内任何非 `visual_assets` 字段（包括 `id` / `display_name` / `type`）
  - `/state/ontology/waystation.json` 顶层结构（`entities[]` 数组本身的元素增删）

# 本任务关键设计决策（不要当 bug 抓）

- **路径 A**（仅扩展数据，不正式化角色 Schema）是 STAGE_1.5_TASKS.md P0.1 锁定决策——**不要建议** reviewer 反过来"新建 character.schema.json"。本体角色对象目前只有 `id` / `display_name` / `type` 三个字段，是**桩形态**，不受 JSON Schema 严格校验，是路径 A 的本意。
- **schema_version 仅新增文件起步 0.2.0**（image_asset.schema.json）；existing `/schema/*.json` 5 个文件 + `/content/test_scene_v0/scene.json` 保持 `0.1.1` 是 P0.5 决策（沿用阶段 1 T-1.0 commit `c47c9cf` "非结构性变更不联动 schema_version" 先例）；**不要建议**反过来联动。
- **ImageAsset 不用 `oneOf` / `anyOf` / `allOf` / `if/then/else` 复合形态**是阶段 1 baseline_001 教训（Gemini schema 子集不接受复合形态；datamodel-code-generator 也有处理坑）。`character_ref` / `location_ref` 与 `target_ref` / `target_type` 的一致性约束**有意**留给 image_validator 语义层（在 SCHEMA_v0.2.md §2.2 + image_asset.schema.json description 中已声明）；**不要建议**在 schema 层用 if/then/else 表达。
- **visual_assets 直接嵌入完整 ImageAsset 对象**（不通过 `asset_id` 引用）是有意——简化 manifest 与本体的结构层次（SCHEMA_v0.2.md §3.2；STAGE_1.5_TASKS.md T-1.5.7 §1811 同源决策）。
- **scene 项暂不加 visual_assets** 是有意——按 synthesis §9.4 推荐方案落地（manifest 用 target_ref 解决）；阶段 2 作者拍板是否反向给 scene/location 也加 `visual_assets[]` 数组，届时 ImageAsset schema **不动**——只在角色/地点本体侧多加一个嵌入路径。
- **`asset_kind` 与 `asset_role` 当前枚举重合**是有意——`asset_kind` 是向后兼容名；`asset_role` 是 Round 5 U-GPT-3 硬闸门，预留未来扩展（`item_icon` / `ui_portrait`）的命名空间。**不要建议**合并删除 `asset_kind`。
- **provenance 字段**（`reference_ids` / `reference_license_note` / `open_source_ok` / `commercial_ok`）是 Round 5 U-GPT-6 软闸门，开源剥离合规预防（C5 共识）；阶段 4 才需要被填准确，但 1.5 阶段就在 schema 里预留位置避免后期回填。**不要建议**移除。
- **schema 层测试不覆盖 character_ref / location_ref 与 target_ref / target_type 的一致性**——这是 image_validator 语义层职责（image_asset.schema.json description §5 + SCHEMA_v0.2.md §2.2 已声明）。本任务 schema 层测试仅覆盖字段存在 / 类型 / 枚举 / 边界 / additionalProperties。

# 已知约束 / 已知遗留 / 不要重复抓

- **本任务不重跑 datamodel-code-generator**（跨边界；推给 T-1.5.3）。`/generator/scripts/regenerate_models.sh` 当前是单 entry（`dialogue_graph.schema.json`）且开头 `find $OUT_DIR -delete`；T-1.5.3 启动时需扩展为多 entry 调用（方案 A 或 B；详见任务提示词 §5）。**finding 不要建议在 T-1.5.2 重跑 codegen**——本任务边界外。
- **本任务不解决** STAGE_1_ACCEPTANCE R1–R8 任何遗留项（不在 1.5 范围；R1–R3 归阶段 2 prompt 调优）。R5（本体污染）虽涉及视觉一致性，但**不是 T-1.5.2 范围**——R5 完整解决在 T-1.5.5 / T-1.5.6（generate_character_sheet 阶段做 character_features 锚定）。
- **本任务不实现 image_validator**——schema 层语义约束（character_ref/location_ref 与 target_ref/target_type 一致性、has_alpha 与 asset_role 一致性、target_ref 闭合性、asset_id 唯一性）的执行落在阶段 1.5 后续任务。本任务仅在 description 中声明这些约束。
- **本任务不动 OpenAI / GPT-Image / ChatGPT** 任何代码；纯 schema + 文档 + schema 层测试；`SAFETY` 维度的 finding 主要看 schema 是否有结构歧义 / pattern 是否过宽。
- **`scene_waystation_of_iron_oath` 暂未加 `visual_assets`** 是有意——按 manifest target_ref 路线落地；不要建议补上。
- **本体角色对象桩形态**（仅 `id` / `display_name` / `type` + 新加 `visual_assets`）是路径 A 决策；不要建议补 schema 文件或正式化字段。

# 配套阅读

- `/docs/SCHEMA_v0.md`（v0.1.x 基线；理解字段命名约定 / `$schema` URL / `$id` 命名 / `additionalProperties: false` 惯例 / `schema_version` 字段位置策略）
- `/docs/SCHEMA_v0.2.md`（本任务核心产出物；§1 增量摘要 / §2 ImageAsset 字段表 + 语义约束 / §3 本体角色实体扩展 / §4 兼容性约束）
- `/docs/HANDOFF_STAGE_1_TO_1.5.md`（特别"⚠️ Schema 扩展警示"段——这是阶段 1.5 例外条款的来源）
- `/docs/STAGE_1.5_TASKS.md`（特别"锁定的架构决策"表 P0.1 路径 A / P0.2 schema_version / P0.3 SCHEMA_v0.2.md / P0.5 不联动 + T-1.5.2 章节）
- `/docs/DECISIONS.md` ADR-014（双模生成策略 + 一致性策略；本任务的姊妹决策）
- `/docs/STAGE_1_ACCEPTANCE.md` §3.1 baseline_001 迭代史（理解 Gemini schema 子集教训：为何不用 oneOf / anyOf / allOf / if-then-else）
- `/schema/dialogue_graph.schema.json` + `node.schema.json`（参考字段命名约定 / `$schema` URL / `schema_version` 字段位置 / `description` 中"留给 /validator 的跨对象约束"语言风格）

# 评审维度（10 类）

每条 finding 必须归到下列一类：

| 代号 | 类别 | 说明 |
|---|---|---|
| **ARCH** | 架构合规 | 是否违反 ADR / 模块边界 / 跨模块写入 |
| **SCHEMA** | Schema 来源 | JSON Schema 仍是单一真相之源；Pydantic / TS 不应手写 |
| **RUNTIME** | 运行时纯净 | `/engine` 仍 0 依赖 LLM；`/generator` 不被运行时 import（ADR-002 / 004） |
| **SAFETY** | 安全 | 输入校验、密钥泄露、注入向量、不安全反序列化、路径遍历 |
| **ERR** | 错误处理 | 边界处校验、内部不过度防御、异常 vs return-result 一致 |
| **TEST** | 测试 | 覆盖关键分支与失败路径；不冗余、不重言；mock 与真实分离 |
| **DEAD** | 冗余 | 死代码、未用 import、过度抽象、为未发生需求设计 |
| **DEPS** | 依赖 | 新增依赖必要、锁版本、许可证风险 |
| **STYLE** | 风格 | 注释只解释 WHY 不解释 WHAT；命名清晰；项目约定遵守 |
| **OPEN** | 开源就绪度 | 作者 hard-code、公开接口稳定性、文档可移植性 |

**避免争论**：
- 不要争 if-elif vs match-case 这类风格题
- 不要建议"加一层抽象更优雅"——本项目反对预先抽象（CLAUDE.md "Don't add features...beyond what the task requires"）
- 不要建议拆文件除非真违反 SRP

# 严重度（3 级）

| 标记 | 级别 | 含义 |
|---|---|---|
| 🔴 | CRITICAL | 必修；违反硬规则 / 安全 / 破坏现有测试 |
| 🟡 | IMPORTANT | 应修；技术债 / 隐蔽 bug / 兼容性风险 |
| 🟢 | NICE | 可选改进，作者拍板是否纳入下一轮 |

**校准**：理想报告应有 ≥ 1 条 🔴 或不报 🔴。若 30+ 行变更全是 🟢，说明审得太松；停下来确认 20afdb6 是否合理。

# 报告产出

**报告路径**：`/docs/reviews/[REPORT_DATE]_T-1.5.2_review.md`
- `[REPORT_DATE]` = 今天日期 `YYYY-MM-DD`
- 若 `/docs/reviews/` 不存在，创建即可（本任务已存在）

**报告结构（严格按这个）**：

```markdown
# Code Review — T-1.5.2: ImageAsset Schema + visual_assets 角色实体扩展（v0.2.0）

**评审者**：GPT-5.5 via Codex
**评审日期**：[REPORT_DATE]
**评审范围**：20afdb6（5 文件 / +782/-11 行）
**项目阶段**：阶段 1.5（视觉资产生成）

---

## 1. 一句话结论
{{1-2 句：质量评价 + 是否阻塞合入}}

## 2. 严重度分布
| 严重度 | 数量 | 边界内可修 | 跨边界 |
|---|---|---|---|
| 🔴 CRITICAL | N | N | N |
| 🟡 IMPORTANT | N | N | N |
| 🟢 NICE | N | N | N |
| **合计** | N | N | N |

## 3. 必修（🔴 CRITICAL）

### 3.1 [{{category}}] {{file}}:{{line}} — {{1 行摘要}}
**问题**：{{2-4 行描述}}
**为什么 CRITICAL**：{{引用 ADR / 规则编号}}
**建议修复**：
\`\`\`python
# 当前
...
# 改为
...
\`\`\`
**模块边界状态**：✅ 在边界内（可修） 或 ⚠️ 跨边界（不要修，记 CLEANUP）

{{重复 3.2 / 3.3 ...}}

## 4. 应修（🟡 IMPORTANT）
{{结构同 §3}}

## 5. 可选改进（🟢 NICE）
{{结构同 §3；建议修复可只给方向不给完整代码}}

## 6. 已知遗留项核对
| Finding | 对应 R 编号 | 出现文件 |
|---|---|---|
{{若无，写"无"}}

## 7. Top 3 行动优先级（按 ROI）
1. {{finding 编号 + 一句理由}}
2. ...
3. ...

## 8. 评审范围外的观察（可忽略）
{{若你看到 20afdb6 之外但值得作者关注的事，写在这里。否则"无"。本节不算 finding}}

## 9. 修复任务指令（**给原 Claude Code 会话**）

> 作者：把下面 ` ```text` 代码块**整段**复制到新 Claude Code 会话作为首条消息。Claude 会读评审报告 + 修代码 + commit + push。

\`\`\`text
你的任务是修复 T-1.5.2 评审中发现的问题。评审报告在 /docs/reviews/[REPORT_DATE]_T-1.5.2_review.md，先读它再开始。

# 必读
1. /docs/reviews/[REPORT_DATE]_T-1.5.2_review.md（评审报告全文；finding 清单 / 建议修复 diff / 边界状态）
2. /CLAUDE.md
3. /docs/STAGE_1.5_TASKS.md T-1.5.2 章节
4. /docs/DECISIONS.md ADR-014（本任务相关 ADR）
5. /docs/SCHEMA_v0.2.md（本任务产出物全文）

# 模块边界（与原 T-1.5.2 任务相同；评审报告 §3/§4/§5 内 ⚠️ 跨边界 项**不要修**）

允许新建 / 修改：
  - /schema/image_asset.schema.json
  - /schema/tests/test_image_asset_schema.py
  - /docs/SCHEMA_v0.2.md
  - /state/ontology/waystation.json（仅 entities[] 中 type=="character" 项的 visual_assets 字段）

严禁修改：
  - /schema/dialogue_graph.schema.json / node.schema.json / option.schema.json / state_effect.schema.json / state_condition.schema.json（任何字段或 schema_version）
  - /content/test_scene_v0/scene.json
  - SCHEMA_v0.md
  - /generator/ / /engine/ / /validator/
  - CLAUDE.md / DECISIONS.md / DEBATE_NOTES.md / HANDOFF_*.md
  - /state/ontology/waystation.json 中既有非 visual_assets 字段或 type=="scene" 项

# 待修 finding 清单

🔴 CRITICAL（必修，全部边界内的）：
{{Codex 自填：从 §3 列出每条 §3.X，仅边界内的；如无写"无"}}

🟡 IMPORTANT（建议全修；如某条修起来风险高 / 不确定，单独提出由作者决定）：
{{Codex 自填：从 §4 列出每条 §4.X，仅边界内的；如无写"无"}}

🟢 NICE（**默认跳过**；如有极简单的 1 行级修复可顺手做；否则不动）：
{{Codex 自填或 "默认跳过"}}

⚠️ 跨边界 / 与 ADR 冲突的 finding（**不要修**；记入 /docs/CLEANUP.md 如不存在则创建）：
{{Codex 自填：从 §3/§4/§5 列出标 ⚠️ 的项；如无写"无"}}

# 修复纪律
- **最小变更**——只修该 finding；不要顺手重构 / 加抽象 / 优化别的
- **保留或新增测试**覆盖修复——修了 bug 必有 test 锁住
- **跑测试通过**——pytest 必绿；测试不通过**不要 commit**，向作者报告
- **每条 finding 单独 commit**，commit message：
  `fix(<module>): <一行修复内容> (review of T-1.5.2 #<finding 编号>)`
  末尾附 Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
- **全部修完后 push**

# 不要做的事
- 不要 disable / skip 测试来通过
- 不要修改 ADR / CLAUDE.md / 任何 schema_version / SCHEMA_v0*.md
- 不要 amend 既有 commit；每个修复一个新 commit
- 不要 push --force；不要修改 git config
- 不要修改跨边界文件（即使评审报告写了建议修复 diff）
- 不要扩展修复范围到报告未列的问题（如发现新 bug，记入 /docs/CLEANUP.md，**不要**直接修；告知作者）
- 不要试图自己重新评审——你的工作是按报告执行修复

# 完成报告
- 已修 finding 列表 + commit hash 各一
- 未修 finding 列表 + 原因（NICE 跳过 / 跨边界已记 CLEANUP / 测试失败 / 其他）
- push 完成确认（git log --oneline -5 输出）
\`\`\`
```

# 完成报告（在对话里给作者）

报告写完后给作者**最多 4 行**：

1. 严重度合计（如 "1🔴 / 3🟡 / 5🟢"）
2. 最严重 1 条 finding 的一句概括
3. 是否阻塞合入（"建议合入"/"建议修后合入"/"建议不合入"）
4. 评审完成 → `/docs/reviews/[REPORT_DATE]_T-1.5.2_review.md`
