# Code Review — PR #36（T-3.11 OSS carve-out v0.2 增量 — 六类 12 条）

**评审者**：GPT-5.5 via Codex  
**评审日期**：2026-05-08  
**评审范围**：PR #36 / `origin/claude/T-3.11-oss-carve-out-v0.2`（统计：1 文件 / 82 行变更；+73 / -9）  
**项目状态**：阶段 3 启动期（阶段 2 已验收通过；T-3.11 属 Wave 0）

---

## 1. 一句话结论

该 PR 基本符合 T-3.11 的文档边界：只改 `/docs/OPEN_SOURCE_CARVE_OUT_INDEX.md`，六类 12 条增量也与任务 prompt 对齐。发现 2 个文档一致性问题；无 CRITICAL，但建议 C 阶段顺手修掉再合入，避免开源剥离清单与 Stage 3 source-of-truth 漂移。

## 2. 严重度分布

| 严重度 | 数量 |
|---|---:|
| 🔴 CRITICAL | 0 |
| 🟡 IMPORTANT | 1 |
| 🟢 NICE | 1 |
| **合计** | **2** |

## 3. 必修（🔴 CRITICAL）

无。

## 4. 应修（🟡 IMPORTANT）

### 4.1 [OPEN] docs/OPEN_SOURCE_CARVE_OUT_INDEX.md:63 — `FORGEWRIGHT_REVIEW_UI_PORT` 默认值与阶段 3 source-of-truth 不一致

**问题**：新增清单把 `FORGEWRIGHT_REVIEW_UI_PORT` 写成“默认值待 T-3.6a 确定”。但 `docs/STAGE_3_TASKS.md` 已经在 §2.4 / §3.4 锁定默认值为 `8765`。开源剥离清单后续会被拿来写 README / `.env.example`，这里继续写“待确定”会让未来文档漏掉已经拍板的默认端口。

**为什么是 IMPORTANT**：C5 的目的就是提前维护开源可移植性边界；环境变量默认值是剥离时最容易被照抄进公开文档的内容。该问题不破坏代码，但会制造 Stage 3 文档之间的漂移。

**修复建议**：

```markdown
# 当前
- `FORGEWRIGHT_REVIEW_UI_PORT`（默认值待 T-3.6a 确定；基于作者本机带宽 / 端口冲突约定）

# 建议
- `FORGEWRIGHT_REVIEW_UI_PORT`（默认 `8765`；本地 localhost 审阅 UI 端口，可由开源用户按端口冲突情况覆盖）
```

## 5. 可选（🟢 NICE）

### 5.1 [STYLE] docs/OPEN_SOURCE_CARVE_OUT_INDEX.md:37 — 维护规则交叉引用写成了错误章节

**问题**：这里写“所有条目继承 §4（v0.1 改 §5）维护规则”，但本 PR 中 §4 是“阶段 3 末期 follow-up 段”，真正的维护规则在 §5。读者会被引到错误段落。

**为什么是 NICE**：这是纯文档导航错误，不影响六类 12 条内容本身；但 C 阶段改一行即可。

**修复建议**：

```markdown
# 当前
所有条目继承 §4（v0.1 改 §5）维护规则

# 建议
所有条目继承 §5（原 v0.1 §3）维护规则
```

## 6. 已核对但不计 finding

- PR 中大量条目引用尚未落地的 T-3.4 / T-3.5 / T-3.6a / T-3.8a 模块；这与 T-3.11 prompt 的 Wave 0 “前瞻 hook”定位一致，不计问题。
- R1.5-1 / R1.5-3 属阶段 1.5 已知遗留，本 PR 只是记录其开源剥离影响，不重复抓。
- 本次未发现 schema、runtime、provider 或模块边界越界：diff 只包含 `/docs/OPEN_SOURCE_CARVE_OUT_INDEX.md`。

## 7. 验证

- 已运行：`git diff --check origin/main...origin/claude/T-3.11-oss-carve-out-v0.2`，无 whitespace error。
- 未运行 pytest：本 PR 为纯文档变更。
