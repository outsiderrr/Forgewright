# Code Review — T-1.5.1A — /generator/CLAUDE.md 历史化阶段 1 禁令 + pyproject 注册 generator.prompts.visual（GPT-5.5 L2 critique §3.4/§4.5 修补）

**评审者**：GPT-5.5 via Codex
**评审日期**：2026-05-01
**评审范围**：4b4a8d9（3 files changed, 290 insertions(+), 2 deletions(-)）
**项目阶段**：阶段 1.5（视觉资产生成）

---

## 1. 一句话结论
主方向正确：阶段 1 禁令已历史化，`generator.prompts.visual` 也已注册；但 `/generator/CLAUDE.md` 仍残留两处会误导下游执行会话的硬规则冲突。建议先修 🔴3.1 / 🔴3.2，再把 T-1.5.1A 视为真正解锁后续 generator 任务的前置补丁。

## 2. 严重度分布
| 严重度 | 数量 | 边界内可修 | 跨边界 |
|---|---|---|---|
| 🔴 CRITICAL | 2 | 2 | 0 |
| 🟡 IMPORTANT | 1 | 0 | 1 |
| 🟢 NICE | 1 | 1 | 0 |
| **合计** | 4 | 3 | 1 |

## 3. 必修（🔴 CRITICAL）

### 3.1 [ARCH] generator/CLAUDE.md:33 — 提交前自检仍绝对禁止触碰 `/state/ontology/`
**问题**：正文第 20 / 26 行已经授权 `image_import` CLI 经 `image_validator` 后写 `entities[].visual_assets`，但提交前自检第 33 行仍写“未触碰 `/state/ontology/`”。下游 T-1.5.7 执行会话会同时收到“必须更新角色 `visual_assets`”和“提交前确认没碰 state”的互斥指令。
**为什么 CRITICAL**：`/generator/CLAUDE.md` 是 generator 任务必读硬规则；该残留禁令会阻断 ADR-014 manual import 契约，并与 ADR-006 的“确定性代码经校验后落库”例外路径冲突。
**建议修复**：
```markdown
# 当前
- 未触碰 `/state/ontology/`

# 改为
- 未触碰 `/state/ontology/`，但阶段 1.5 已授权的 `image_import` CLI 例外除外：仅可经 `image_validator` 校验后写 `/state/ontology/waystation.json` 的 `entities[].visual_assets`，不得改任何非 `visual_assets` 字段。
```
**模块边界状态**：✅ 在边界内（可修）

### 3.2 [ARCH] generator/CLAUDE.md:23 — 图像预算规则指向已废弃/未定义的 `image_budget.check_and_charge()`
**问题**：第 23 行要求所有 image API 调用走 `image_budget.check_and_charge()`；但阶段 1.5 任务清单中 T-1.5.5 已把图像预算拆为 `image_budget.check()` + `image_budget.log_charge()`，并说明原统一 `check_and_charge` 接口被废弃。第 11 行的“任何 API 调用前必须经 `budget.check_and_charge()`”也仍是文本 LLM 规则，未把 image API 排除出来，容易让 T-1.5.9 同时接错文本预算和不存在的图像预算 API。
**为什么 CRITICAL**：ADR-012/014 的核心是预算硬卡；硬规则指向错误 API 会直接让后续实现失败，或诱导执行会话绕过 T-1.5.5 已设计好的 check/log 顺序。
**建议修复**：
```markdown
# 当前
- **任何 API 调用前必须经 `budget.check_and_charge()` 拦一次**（ADR-012）。无例外，包括重试、调试脚本。超额抛 `BudgetExceeded`。
...
  - 任何 image API 调用前必须经 `image_budget.check_and_charge()`（ADR-012 + ADR-014）

# 改为
- **任何文本 LLM API 调用前必须经 `budget.check_and_charge()` 拦一次**（ADR-012）。无例外，包括重试、调试脚本。超额抛 `BudgetExceeded`。
...
  - 任何 image API 调用前必须经 T-1.5.5 定义的 `image_budget.check()` 预算拦截；provider 成功返回后用 `image_budget.log_charge()` 记账（ADR-012 + ADR-014）
```
**模块边界状态**：✅ 在边界内（可修）

## 4. 应修（🟡 IMPORTANT）

### 4.1 [STYLE] docs/reviews/_prompts/T-1.5.1A_codex_review.md:43 — 评审 prompt 仍残留 `[REVIEW_COMMIT]` / `[REVIEW_STATS]`
**问题**：该 prompt 文件是工作流强制产物，但第 43 / 45 / 49 / 87 / 135 / 184 行仍保留 `[REVIEW_COMMIT]` 或 `[REVIEW_STATS]`。作者若直接复制此文件给 Codex，会得到 `git show [REVIEW_COMMIT]` 这种不可执行目标；这破坏了“paste-ready”的承诺。
**为什么 IMPORTANT**：不影响已手工补齐上下文的本次评审，但会破坏同一 commit 内交付的复用评审入口；对不会编程的作者来说，这是实际工作流风险。
**建议修复**：
```markdown
# 当前
- **Commit**：[REVIEW_COMMIT]
- **Statistics**：[REVIEW_STATS]
执行 `git show [REVIEW_COMMIT]` ...

# 改为
- **Commit**：4b4a8d9
- **Statistics**：3 files changed, 290 insertions(+), 2 deletions(-)
执行 `git show 4b4a8d9` ...
```
并替换文件内其余 `[REVIEW_COMMIT]` / `[REVIEW_STATS]` 出现处。
**模块边界状态**：⚠️ 跨边界（当前修复任务严禁改 `/docs/`；不要在 T-1.5.1A 修复会话内动这个文件，另记 CLEANUP 或另开 docs 边界任务）

## 5. 可选改进（🟢 NICE）

### 5.1 [STYLE] generator/CLAUDE.md:7 — 标题仍写“补充阶段 1 三条 ADR”
**问题**：标题现在下方已包含 ADR-014 / ADR-015 的 1.5 规则，但仍写“补充阶段 1 三条 ADR”。这不是功能错误，只是以后读者扫标题时会低估该文件已经覆盖阶段 1.5。
**为什么 NICE**：可读性问题，不阻塞实现。
**建议修复**：
```markdown
# 当前
## 硬性约束（继承自 /CLAUDE.md，并补充阶段 1 三条 ADR）

# 改为
## 硬性约束（继承自 /CLAUDE.md，并补充阶段 1 / 1.5 ADR）
```
**模块边界状态**：✅ 在边界内（可修）

## 6. 已知遗留项核对
无

## 7. Top 3 行动优先级（按 ROI）
1. 3.1 — 立刻消除 `image_import` 写 `visual_assets` 与提交前自检的硬冲突。
2. 3.2 — 立刻修正 image budget API 名称/调用顺序，避免 T-1.5.5/1.5.6/1.5.9 被错误硬规则带偏。
3. 4.1 — 跨边界记录 prompt placeholder 漏替换；不在本修复会话内改 docs，但要避免同类工作流问题复发。

## 8. 评审范围外的观察（可忽略）
当前 `main` 在 4b4a8d9 之后已有后续提交为 `STAGE_1.5_TASKS.md` 增加 placeholder 自检；本报告仍按 4b4a8d9 的三文件 diff 给 finding，不把后续 plan 修订当作本次改动。

## 9. 修复任务指令（**给原 Claude Code 会话**）

> 作者：把下面 ` ```text` 代码块**整段**复制到新 Claude Code 会话作为首条消息。Claude 会读评审报告 + 修代码 + commit + push。

```text
你的任务是修复 T-1.5.1A 评审中发现的问题。评审报告在 /docs/reviews/2026-05-01_T-1.5.1A_review.md，先读它再开始。

# 必读
1. /docs/reviews/2026-05-01_T-1.5.1A_review.md（评审报告全文；finding 清单 / 建议修复 diff / 边界状态）
2. /CLAUDE.md
3. /docs/STAGE_1.5_TASKS.md T-1.5.1A 章节
4. /docs/DECISIONS.md（特别 ADR-006 / ADR-011 / ADR-012 / ADR-014 / ADR-015）

# 模块边界（与原 T-1.5.1A 任务相同；评审报告 §3/§4/§5 内 ⚠️ 跨边界 项**不要修**）
只允许修改：
  - /generator/CLAUDE.md
  - /pyproject.toml
严禁修改：
  - /CLAUDE.md（规则 9）
  - /docs/、/schema/、/state/、/engine/、/validator/、/content/
  - /generator/ 下任何 .py（本任务纯文档/配置）
  - 任何 /schema/*.json
  - 任何 /generator/models/_generated/

# 待修 finding 清单

🔴 CRITICAL（必修，全部边界内的）：
3.1 [ARCH] generator/CLAUDE.md:33 — 提交前自检仍绝对禁止触碰 /state/ontology/；修成只禁止未授权 state 改动，并明确 image_import 经 image_validator 后只可写 entities[].visual_assets。
3.2 [ARCH] generator/CLAUDE.md:23 — 图像预算规则指向已废弃/未定义的 image_budget.check_and_charge()，且文本 budget 规则过宽；修成文本 LLM 用 budget.check_and_charge，image API 用 T-1.5.5 的 image_budget.check() + log_charge()。

🟡 IMPORTANT（建议全修；如某条修起来风险高 / 不确定，单独提出由作者决定）：
无边界内 IMPORTANT。

🟢 NICE（**默认跳过**；如有极简单的 1 行级修复可顺手做；否则不动）：
5.1 [STYLE] generator/CLAUDE.md:7 — 标题仍写“补充阶段 1 三条 ADR”；可顺手改为“阶段 1 / 1.5 ADR”。

⚠️ 跨边界 / 与 ADR 冲突的 finding（**不要修**；记入 /docs/CLEANUP.md 如不存在则创建）：
4.1 [STYLE] docs/reviews/_prompts/T-1.5.1A_codex_review.md:43 — 评审 prompt 仍残留 [REVIEW_COMMIT] / [REVIEW_STATS]；当前修复任务严禁改 /docs/，不要在本会话修，记录 CLEANUP 或请作者另开 docs 边界任务。

# 修复纪律
- **最小变更**——只修该 finding；不要顺手重构 / 加抽象 / 优化别的
- **保留或新增测试**覆盖修复——本任务是纯文档 + 配置，TEST 维度通常不涉及；如评审标注需补 schema/格式校验，作为最小补充
- **跑测试通过**——pytest 必绿（如有相关测试）；测试不通过**不要 commit**，向作者报告
- **每条 finding 单独 commit**，commit message：
  `fix(<module>): <一行修复内容> (review of T-1.5.1A #<finding 编号>)`
  末尾附 Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
- **全部修完后 push**

# 不要做的事
- 不要 disable / skip 测试来通过
- 不要修改 ADR / CLAUDE.md / 任何 schema_version / SCHEMA_v0*.md
- 不要 amend 既有 commit；每个修复一个新 commit
- 不要 push --force；不要修改 git config
- 不要修改跨边界文件（即使评审报告写了建议修复 diff）
- 不要扩展修复范围到报告未列的问题（如发现新 bug，记入 /docs/CLEANUP.md，**不要**直接修；告知作者）
- 不要试图自己重新评审——你的工作是按报告执行修复
- 不要在本修复任务里加 Pillow / openai 等依赖（这些是 T-1.5.4 / T-1.5.9 的职责，越界）

# 完成报告
- 已修 finding 列表 + commit hash 各一
- 未修 finding 列表 + 原因（NICE 跳过 / 跨边界已记 CLEANUP / 测试失败 / 其他）
- push 完成确认（git log --oneline -5 输出）
```
