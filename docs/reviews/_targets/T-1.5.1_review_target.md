# Review Target — T-1.5.1 — ADR-014 + Stage 1.5 ROADMAP fill + SCHEMA_v0.2 scaffold

**Commit**: this commit（解析方式：`git log -1 --format=%H`，本评审准备文件捎带在同一主 commit 内）
**Branch**: main
**Statistics**: 7 文件 / 136 insertions(+) / 4 deletions(-)（见 `git diff --stat HEAD~1 HEAD`）

## 1. 模块边界（本任务被授权改的范围）

允许修改 / 新建：

- `/docs/DECISIONS.md`（追加 ADR-014；不修改既有 ADR-001…013）
- `/docs/ROADMAP.md`（替换"阶段 1.5"段的"完成标志 / 重点工作 / 禁止事项 / 依赖"四子段；追加"更新记录"一行）
- `/docs/SCHEMA_v0.2.md`（**新建**占位文件；正式内容由 T-1.5.2 填）
- `/content/visuals/_reference/.gitkeep`（**新建**目录占位）
- `/content/visuals/CLAUDE.md`（**新建**模块级指引）
- `/.gitignore`（追加视觉资产相关 4 条规则）
- `/docs/reviews/_targets/T-1.5.1_review_target.md`（**新建**本文件；评审准备）

严禁修改（本任务**未触**）：`CLAUDE.md`、`SCHEMA_v0.md`、`DEBATE_NOTES.md`、`HANDOFF_STAGE_1_TO_1.5.md`、`STAGE_1_ACCEPTANCE.md`、任何代码模块、任何 `/schema/*.json`、`/content/test_scene_v0/`。

## 2. 本任务关键设计决策（reviewer 应给予权重，不要当 bug 抓）

- **修改 DECISIONS.md 是 CLAUDE.md 规则 10 的明示例外**：作者已于 2026-04-30 阶段 1.5 规划会话明确授权追加 ADR-014（已在 DECISIONS.md "变更历史"段落留痕）。本任务只追加，不修改既有 ADR。
- **ADR-014 与已有 ADR-011（LLMProvider 可插拔）保持兼容但不复用**：`ImageProvider` 是新接口，与 `LLMProvider` 同级、互不继承。这是有意设计——文本生成与图像生成的语义、成本、重试策略差异显著，强行抽象一个共同基类会造成接口污染。
- **SCHEMA_v0.2.md 是占位文件，不应包含 ImageAsset 字段定义**：字段细节由 T-1.5.2 落地。本任务只产出"骨架"（4 个章节标题 + 兼容性约束 + 版本段）。reviewer 看到 §1/§2/§3 是占位是预期，不是 bug。
- **`/content/visuals/_reference/` 内容 gitignore 是版权考量，不是 size 问题**：作者从 Pinterest / 自购获取的视觉风格基准图可能受版权保护，不能进公开仓库。目录本身用 `.gitkeep` 入 git 是为了让 `_reference/` 路径在 clone 后即存在，作者 / 开源用户可立即放图。
- **`schema_version` 在本任务内**完全不动**：本任务没有触 `/schema/*.json` 任何文件。SCHEMA_v0.2.md 的占位文件提到"仅新增文件 image_asset.schema.json 起步 0.2.0；existing /schema/*.json + scene.json 保持 0.1.1"——这只是**预告**，实际改动在 T-1.5.2。
- **ROADMAP "阶段概览"表格**未动：表格的 1.5 行已存在（commit 1d2030f 引入），本任务仅替换四个子段实质内容。
- **ADR-014 落地不规划任务拆分**：任务拆分是 STAGE_1.5_TASKS.md 的事，ADR 只记决策本身。

## 3. 已知约束 / 已知遗留 / 不要重复抓

- **本任务不实现任何代码**：纯文档 + .gitignore；无 Python / TS / JSON Schema 改动。
- **本任务不解决阶段 1 R1–R8 任何遗留项**：R1–R3 是阶段 2 的 prompt 调优；R5（本体污染）/ R7（cost_log 高估）/ R8（机械预检器）会在 1.5 后续任务（T-1.5.2 / T-1.5.4 / T-1.5.5）落地，本任务只产文档。
- **ROADMAP "阶段概览"表格行（1.5 行）已存在**：不重复添加；reviewer 不要建议"补一行"。
- **ImageAsset 字段定义不在本任务范围**：reviewer 不要建议"在 SCHEMA_v0.2.md §2 补一段字段表"——那是 T-1.5.2。
- **不实现 ImageProvider 接口、不实现 manual / API provider、不动 /schema/**：均是 1.5 后续任务（T-1.5.2 / T-1.5.3 / T-1.5.9）。
- **本评审准备文件的 commit hash 字段在 commit 完成时才解析**：本文件捎带在同一主 commit 内，hash 在 commit 落地前不存在，reviewer 用 `git log -1` 可解析。

## 4. 配套阅读（在 REVIEW_PROMPT_CODE_GPT.md 必读列表外，本任务额外要读的）

- `/docs/HANDOFF_STAGE_1_TO_1.5.md`（特别"阶段 1.5 启动条件"段 + "⚠️ Schema 扩展警示"段——理解为何动 DECISIONS.md 和动 Schema 都属授权例外）
- `/docs/STAGE_1.5_TASKS.md` "锁定的架构决策"表（理解 ADR-014 与本规划其他决策的串接关系）+ "T-1.5.1" 章节（本任务原始提示词）
- `/docs/DECISIONS.md` ADR-011 / ADR-012（ADR-014 的姊妹决策；格式参照）
- `/CLAUDE.md` 规则 2 / 9 / 10（理解为何这些通常禁止的修改在本任务内被授权）

## 5. 一句话概述本次变更

落地阶段 1.5 的纯文档预备：追加 ADR-014（视觉资产双模生成策略 + GPT-Image 默认 + C+B 一致性兜底）、把 ROADMAP 阶段 1.5 的四个子段从粗略提案补成可验收的实质内容、新建 SCHEMA_v0.2.md 占位（字段细节交 T-1.5.2）、创建 `/content/visuals/_reference/` 目录约定 + 模块级 CLAUDE.md、加固 .gitignore 阻断版权资产 / 临时产物 / 图像 cost_log 入库。
