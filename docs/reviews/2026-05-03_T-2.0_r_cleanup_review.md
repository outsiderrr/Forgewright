# Code Review — PR Forgewright#9 commit 31690c0（T-2.0 R cleanup PATCH）

**评审者**：GPT-5.5 via Codex
**评审日期**：2026-05-03
**评审范围**：PR Forgewright#9 commit 31690c0（分支 claude/sweet-colden-ff1fed，base=main；统计：10 文件 / +555 / -37）
**项目状态**：阶段 2 起手 cleanup gate（R2/R3/R4）

---

## 1. 一句话结论

整体边界守得住：diff 只落在 `/generator/`，未改 schema/runtime/validator，R2 复合 condition 和 R4 字段迁移主干基本到位。但 R3 few-shot 仍含超长旧选项、R4 的 Aelwin fixture 主地点提示错误，会继续削弱本次 cleanup 的核心目标；建议修完再合。

## 2. 严重度分布

| 严重度 | 数量 |
|---|---|
| 🔴 CRITICAL | 0 |
| 🟡 IMPORTANT | 2 |
| 🟢 NICE | 0 |
| **合计** | **2** |

## 3. 必修（🔴 CRITICAL）

无。

## 4. 应修（🟡 IMPORTANT）

### 4.1 [STYLE] generator/prompts/few_shot.py:138 — R3 硬约束被旧 few-shot 长选项抵消

**问题**：`load_iron_oath_few_shot()` 现在仍把 5 个原始《铁誓驿站》节点原样放在前面，再追加 2 个新 composite demo。原始节点里至少 3 条 `Option.text` 明显超过 25 字：`content/test_scene_v0/scene.json:34`、`:94`、`:116`。这会让模型同时看到 system prompt 说“超长 = schema_invalid”和 few-shot 说“长选项是期望输出”，R3 cleanup 的信号互相打架。

**为什么是 IMPORTANT**：T-2.0 明确要把 R3 从“≤25 优先”升级成硬约束；few-shot 是 prompt 的强示范层，若示范本身违反约束，阶段 1 的选项过长问题很可能继续复现。

**修复建议**：
```python
# 当前
scene_pairs = [
    FewShotPair(
        input_context=_render_input_context(graph, nid),
        expected_node=graph["nodes"][nid],
    )
    for nid in order
]
return scene_pairs + load_composite_condition_few_shot()

# 建议方向：不要改 content gold scene；在 few-shot copy 里缩短/替换超长 option text，
# 并加测试覆盖 load_iron_oath_few_shot() 的全部 7 个示例，而不是只测新增 2 个 demo。
```

### 4.2 [ERR] generator/experiment.py:175 — Aelwin fixture 的 primary_location_ref 指向了错误地点

**问题**：`dialogue_middle_aelwin` 的 `narrative_intent` 写的是“玩家潜行抵达牧人废屋，与逃兵 Aelwin 对视”（同文件 `:183`），但 `primary_location_ref` 仍复用全局 `_PRIMARY_LOCATION_REF = scene_waystation_of_iron_oath`。`assemble_context_block()` 会把它渲染成“主地点（推荐默认 location_ref）”，这会诱导模型把 Aelwin 节点继续写回驿站。

**为什么是 IMPORTANT**：R4 的目标就是降低 `location_ref` 错配；这个 fixture 正好覆盖原先最容易错的 Aelwin 场景，却把“推荐默认地点”设成了与叙事意图冲突的地点。

**修复建议**：
```python
# 当前
Fixture(
    fixture_id="dialogue_middle_aelwin",
    graph_context=GraphContext(
        scene_anchor="scene_waystation_of_iron_oath",
        location_candidates=_LOCATION_CANDIDATES,
        primary_location_ref=_PRIMARY_LOCATION_REF,
        ...
    ),
)

# 建议
Fixture(
    fixture_id="dialogue_middle_aelwin",
    graph_context=GraphContext(
        scene_anchor="scene_waystation_of_iron_oath",
        location_candidates=_LOCATION_CANDIDATES,
        primary_location_ref="scene_eastern_pasture_ruin",
        ...
    ),
)
```

## 5. 可选改进（🟢 NICE）

无。

## 6. 已知遗留项核对

阅读 `/docs/STAGE_1_ACCEPTANCE.md` §4 后，本次评审中遇到的下列 finding **属已知 R 项**，不重复列入正文：

| Finding | 对应 R 编号 | 出现文件 |
|---|---|---|
| 无 | 无 | 无 |

## 7. Top 3 行动优先级

按“修起来 ROI 最高”排序：

1. 4.1：先让 few-shot 全部遵守 ≤25，否则 R3 的硬约束会被示例抵消。
2. 4.2：修正 Aelwin fixture 的主地点，直接补上 R4 最关键的回归风险。
3. 无第三项。

## 8. 评审范围外的观察（可忽略）

未重新跑 `pytest`，以避免只读评审中产生缓存文件；本次做了 `git diff --check main..31690c0`，无 whitespace error。`location_card` 残留仅见于视觉管线 `VisualGenerationContext.location_card` 和负向测试/注释文本，视觉管线字段语义独立，不计 finding。
