# Code Review — PR #39（T-3.1 ADR-022~026 立项；commit 39fcbd1）

**评审者**：GPT-5.5 via Codex
**评审日期**：2026-05-08
**评审范围**：PR #39 / `origin/claude/exciting-williamson-e221f7` / commit `39fcbd1`（统计：1 文件 / 175 行变更）
**项目状态**：阶段 3 启动期（阶段 2 已验收通过；T-3.1 为 Wave 1 ADR 立项）

---

## 1. 一句话结论

5 条 ADR 的编号顺序、模块边界、作者授权记录，以及 ADR-023~026 的关键锁定项总体合规；但 ADR-022 对 `STAGE_3_TASKS.md` v1.0 §3.1 的 playtest calibration / worst path 输出要求有一处缩写遗漏，建议 C 阶段补齐后再合入。

## 2. 严重度分布

| 严重度 | 数量 |
|---|---:|
| 🔴 CRITICAL | 0 |
| 🟡 IMPORTANT | 1 |
| 🟢 NICE | 0 |
| **合计** | **1** |

## 3. 必修（🔴 CRITICAL）

无。

## 4. 应修（🟡 IMPORTANT）

### 4.1 [ARCH] docs/DECISIONS.md:522 — ADR-022 漏掉 calibration 降档条件与 worst_paths 必备字段

**问题**：`STAGE_3_TASKS.md` v1.0 §3.1 line 203 锁定：calibration smoke 后如果 `1 path` 平均 `5+ calls`，必须调整 `M` 上限或改 worst-bucket 抽样形态。PR 当前 ADR-022 只写“实测后再锁 5×20 参数”，漏掉触发条件和应对动作。同时 §3.1 line 211 锁定 `worst_paths.jsonl` 至少含 `path trace + judge_score + critical_count + severity`，当前 line 525 只写“path 级”，下游 T-3.4 可能产出不可审计的 worst path 文件。

**为什么是 IMPORTANT**：这不是措辞偏好，而是 F9 / F21 的成本治理与审计契约。三重 guard 能中止失控批次，但缺少 calibration 后的降档规则会让执行会话在 smoke 已显示过贵时仍“合规地”锁 5×20；缺少 path 字段会削弱 critical/major/minor 复核能力。

**修复建议**：

```markdown
# 当前
- **calibration run 必做（F9）**：T-3.4 A 阶段 mandatory smoke = 1 scene × 1 persona × 5 paths 实测 avg calls/path / tokens/path / seconds/path / cost/path；实测后再锁 5×20 参数
...
- **双层输出（F21）**：`playtest_NNN/worst_paths.jsonl`（path 级）+ `playtest_NNN/worst_scenes.md` + `worst_scenes.json`（scene 级；scene 分数 = path 分布 / critical count / 最低分加权）

# 建议
- **calibration run 必做（F9）**：T-3.4 A 阶段 mandatory smoke = 1 scene × 1 persona × 5 paths 实测 avg calls/path / tokens/path / seconds/path / cost/path；实测后再锁 5×20 参数；如 1 path 平均 5+ calls（每决策节点 + judge），调整 M 上限或 worst-bucket 抽样形态
...
- **双层输出（F21）**：
  - `playtest_NNN/worst_paths.jsonl`（path 级；含 path trace + judge_score + critical_count + severity）
  - `playtest_NNN/worst_scenes.md` + `worst_scenes.json`（scene 级；scene 分数 = path 分布 / critical count / 最低分加权）
```

## 5. 可选（🟢 NICE）

无。

## 6. 已核对通过

| 项 | 结论 |
|---|---|
| ADR 编号与顺序 | ADR-022 → ADR-026 顺序正确，插入位置在 ADR-021 后、变更历史前 |
| 模块边界 | diff 仅修改 `/docs/DECISIONS.md`，符合 T-3.1 边界 |
| 作者授权记录 | 变更历史 line 692 明示作者授权新增 ADR-022~026，符合 CLAUDE.md 规则 10 例外 |
| ADR-023 | 明示 `context assembly over-approx trace`，不是 scene 反查；含五命名空间 pattern、`uniqueItems`、`scene_id` pattern、optional missing-only |
| ADR-024 | 使用 `SceneGraphContext`，不是 `GraphContext`；含 `prior_scene_summaries` 与四个 token/prompt metrics hook 字段 |
| ADR-025 | 含 T-3.6a/b 拆分、FastAPI/uvicorn deps、`pyproject.toml` 模块边界、mermaid fallback、browser smoke mandatory |
| ADR-026 | 含 SceneSpec DAG、RateLimitedProvider wrapper、拓扑分层、写入顺序、T-3.5 不依赖 T-3.4 |

## 7. 非问题说明

- 本次是 ADR 文档立项，不要求同时修改 `/schema/`、`/generator/`、`/tools/` 或 `pyproject.toml`；这些属于后续 T-3.2 / T-3.3 / T-3.4 / T-3.5 / T-3.6 / T-3.7 范围。
- 阶段 1 R1–R8 与阶段 2 R2-* 遗留没有被重复计入 finding；本次只抓 PR #39 新增 ADR 与 Stage 3 v1.0 锁定核心之间的偏差。
