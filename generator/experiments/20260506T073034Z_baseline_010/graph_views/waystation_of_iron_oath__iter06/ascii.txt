entry: node_arrival

┌───────────────────┐
│ node_arrival      │
│ dia · char_vellin │
└───────────────────┘

  │
  ▼

┌─────────────────────┐  ┌─────────────────────┐  ┌────────────────────────┐
│ node_reception_warm │  │ node_reception_cold │  │ node_reception_evasive │
│ dia · char_vellin   │  │ dia · char_vellin   │  │ dia · char_vellin      │
└─────────────────────┘  └─────────────────────┘  └────────────────────────┘

  │
  ▼

┌──────────────────────┐
│ node_flaw_discovered │
│ dia · char_vellin    │
└──────────────────────┘

  │
  ▼

┌─────────────────────┐
│ node_push_for_truth │
│ dia · char_vellin   │
└─────────────────────┘

  │
  ▼

╔═══════════════╗  ╔══════════════╗
║ end_complicit ║  ║ end_expose   ║
║ end · (narr)  ║  ║ end · (narr) ║
╚═══════════════╝  ╚══════════════╝

edges:
  node_arrival → node_reception_warm
  node_arrival → node_reception_cold
  node_arrival → node_reception_evasive  [cond, unav=disable_with_hint]
  node_arrival → node_reception_evasive
  node_reception_warm → node_push_for_truth
  node_reception_warm → node_push_for_truth
  node_reception_warm → node_flaw_discovered
  node_reception_warm → node_flaw_discovered  [cond, unav=disable_with_hint]
  node_reception_cold → node_push_for_truth
  node_reception_cold → node_flaw_discovered  [cond, unav=disable_with_hint]
  node_reception_cold → node_push_for_truth  [cond, unav=disable_with_hint]
  node_reception_cold → node_push_for_truth
  node_reception_evasive → node_flaw_discovered
  node_reception_evasive → node_push_for_truth
  node_reception_evasive → node_push_for_truth  [cond, unav=disable_with_hint]
  node_flaw_discovered → end_expose
  node_flaw_discovered → end_complicit
  node_flaw_discovered → node_push_for_truth
  node_flaw_discovered → node_push_for_truth  [cond, unav=disable_with_hint]
  node_push_for_truth → end_complicit
  node_push_for_truth → end_expose
  node_push_for_truth → end_complicit
  node_push_for_truth → end_complicit  [cond, unav=disable_with_hint]

end nodes: end_complicit, end_expose
