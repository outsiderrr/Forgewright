# engine 模块补充规则

## 本模块职责（一句话）

读取 JSON 对话图的极薄、确定性运行时播放器。

## 硬性约束（继承自 /CLAUDE.md）

- 严禁任何 LLM 调用与网络依赖（见 /CLAUDE.md 规则 3、ADR-002、ADR-004）。
- 代码量目标 ≤ 500 行；不接入任何编剧理论逻辑（见 ADR-005）。

## TODO

阶段 0 后期补充：节点类型覆盖、输入/输出接口、与 /state 的只读边界定义。
