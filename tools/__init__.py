"""Forgewright 作家工坊辅助工具命名空间（CLAUDE.md §仓库结构 / `/tools/`）。

阶段 3 起步首批入驻成员（ADR-025 / ADR-023 联动）：

- `tools.dep_propagate`：基于 ContentDependencyIndex sidecar 的反向 propagate 工具
  （T-3.7；ADR-023 一致性维护）。

后续 T-3.6a / T-3.6b 落地 `tools.review_ui.*`（ADR-025）。
"""
